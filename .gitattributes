# Normalize text files to LF in repo
* text=auto eol=lf

# Treat binary assets correctly
*.pdf binary
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.svg text eol=lf

# Lock large artifacts if using Git LFS in the future (commented for now)
# *.pdf filter=lfs diff=lfs merge=lfs -text

